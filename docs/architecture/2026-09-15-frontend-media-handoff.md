# 创作页媒体展示接续记录

日期：2026-09-15。

## 本轮范围

继续 `/Users/huangnaiwen/project/ai-frontend-service` 的前端重构，不改旧项目或 Go 后端，不初始化 Git，不访问生产、真实生成、支付和 OAuth。

- `CreationImageInput` 负责本地选图预览、移除和对象地址释放，不上传文件、不创建任务。
- 模板编辑和视频创作复用选图组件。视频移除首帧后可以继续按描述提交，仍由 Go 编排首帧与视频两步生成。
- `CreationResult` 统一文生图、模板编辑和视频的完成态展示。视频使用原生播放控制，不自动播放；媒体完整展示，限制高度。
- 模板编辑补齐已有审核没收终态的提示。审核态不展示返回的媒体地址；作品加载失败不重建任务、不改变生成终态。
- 保留原有提交门禁、上传时机、服务端素材标识和错误码处理。组件职责与对象地址生命周期使用中文注释。

## 验证

- 回归测试先运行：首批新增 9 项中 8 项因缺少功能失败，视频审核态的既有行为 1 项通过；独立审查后补充 4 项边界测试，最终全套 20 个文件、163 项测试通过。
- TypeScript 两个项目分别使用 `--noEmit` 检查通过。
- Vite 构建、5 个公共页预渲染、Go-only API 审计通过。
- 受限环境无法写入原项目 `node_modules/.tmp`。验证时使用临时缓存和构建目录，不覆盖旧 `dist`；未把权限错误当作代码缺陷。
- Playwright 在 390 × 844 和 1440 × 1000 下验证模板编辑及视频创作：选图前后不发生 POST，移除可重新选择，提交仅上传与创建各一次，成品图片解码成功，视频可播放，无页面异常和横向溢出。
- 浏览器 API 全部拦截为本地协议桩，图片来自已复制的静态资源，视频为测试画面。没有真实生成、扣钻或目录写入。这不是 Go 端到端业务验收，也不是旧页面视觉等价验收。
- 独立代码审查未发现阻塞问题；建议补充的直接换图、缺地址和新地址重置错误状态均已纳入回归测试。

## 未完成与下一步

本记录补充 `2026-09-14-frontend-handoff.md`，不代表整站迁移完成。

1. 继续核对旧源码、本地快照中的真实模板 ID、封面、预览、分类、顺序和启用状态。当前 Go 本地目录仍为测试数据，不能用测试封面冒充业务封面。
2. 还原旧模板预览弹层及创作页完整布局，继续同尺寸视觉对照；本轮只补素材和结果的基础展示。
3. 未登录首页、登录注册、用户中心、设置、钱包、作品、通知、反馈仍需逐页完整对照。
4. 公共内容、语言路由、动态 SEO、PWA 和其它仍使用的旧入口仍需核查。Animate 与管理后台继续排除。

临时验收目录：`/private/tmp/ai-frontend-preview-ePMVsQ`，包含回归候选、浏览器脚本、截图和测试视频。不要把临时媒体或协议桩复制到业务目录。
